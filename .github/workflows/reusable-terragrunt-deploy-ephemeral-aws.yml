name: Deploy Terragrunt Sandbox Ephemeral Environment
on:
  workflow_call:
    inputs:
      git_branch:
        description: "Branch triggering this deploy."
        required: true
        type: string
      tf_version:
        description: "Version of Terraform to utilize"
        required: true
        default: "1.5.5"
        type: string
      tg_version:
        description: "Version of Terragrunt to utilize"
        required: true
        default: "0.54.11"
        type: string
      assume_role_arn:
        description: "ARN of the role to assume prior to Terragrunt invocation. Terragrunt may use this role to assume other roles if configured to do so."
        required: true
        type: string
      region:
        description: "Region within the environment (e.g. us-east-1) to deploy"
        required: true
        type: string
      environments_root:
        description: "Root folder containing all environments"
        required: false
        default: "platform"
        type: string
      env_id:
        description: "Instance within the region (e.g. 000) to deploy."
        required: true
        type: string
      before_plan_commands:
        description: "Commands to run prior to executing Terragrunt plan."
        required: false
        type: string
        default: ""
      before_deploy_commands:
        description: "Commands to run prior to executing Terragrunt apply."
        required: false
        type: string
        default: ""
      before_shared_commands:
        description: "Commands to run prior to both Terragrunt plan and apply. These are applied after before_plan_commands and before_deploy_commands."
        required: false
        type: string
        default: ""
      after_plan_commands:
        description: "Commands to run after executing Terragrunt plan."
        required: false
        type: string
        default: ""
      after_deploy_commands:
        description: "Commands to run after executing Terragrunt apply."
        required: false
        type: string
        default: ""
      after_shared_commands:
        description: "Commands to run after both Terragrunt plan and apply. These are applied after after_plan_commands and after_deploy_commands."
        required: false
        type: string
        default: ""
    outputs:
      terragrunt_plan_output:
        description: "String containing all Terragrunt output from the plan step"
        value: ${{ jobs.deploy.outputs.terragrunt_plan_output}}
      terragrunt_deploy_output:
        description: "String containing all Terragrunt output from the deployment step"
        value: ${{ jobs.deploy.outputs.terragrunt_deploy_output }}
      terraform_outputs:
        description: "JSON string containing all Terraform outputs from the deployment (base64 encoded)"
        value: ${{ jobs.deploy.outputs.terraform_outputs_json }}

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: "Plan & Deploy ephemeral/${{ inputs.region }}/${{ inputs.env_id }}"
    runs-on: ubuntu-latest
    outputs:
      terragrunt_plan_output: ${{ steps.plan.outputs.tg_action_output }}
      terragrunt_deploy_output: ${{ steps.deploy.outputs.tg_action_output }}
      terraform_outputs_json: ${{ steps.deploy.outputs.terraform_outputs_json }}
    steps:
      - name: Checkout
        uses: actions/checkout@8edcb1bdb4e267140fa742c62e395cd74f332709

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@61815dcd50bd041e203e49132bacad1fd04d2708
        with:
          role-to-assume: ${{ inputs.assume_role_arn }}
          role-session-name: ${{ github.run_id }}
          aws-region: ${{ inputs.region }}

      - name: "Set default Terraform Tags"
        id: set-tags
        run: |
          set -x
          echo "TF_VAR_organization_tag=${{ github.repository_owner }}" >> "$GITHUB_OUTPUT"
          echo "TF_VAR_repository_tag=$(echo "${{ github.repository }}" | cut -d "/" -f 2)" >> "$GITHUB_OUTPUT"
          echo "TF_VAR_commit_hash_tag=${{ github.sha }}" >> "$GITHUB_OUTPUT"
          echo "TF_VAR_branch_tag=${{ inputs.git_branch }}" >> "$GITHUB_OUTPUT"

      - name: Create mise.toml
        id: create-mise-toml
        run: |
          echo '[tools]' > mise.toml
          echo 'terraform = "${{ inputs.tf_version }}"' >> mise.toml
          echo 'terragrunt = "${{ inputs.tg_version }}"' >> mise.toml

      - name: Plan
        id: plan
        uses: gruntwork-io/terragrunt-action@v3.1.0
        env:
          AWS_REGION: ${{ inputs.region }}
          INPUT_PRE_EXEC_0: |
            sudo apt update -yqq && sudo apt install python3 -yqq
          INPUT_PRE_EXEC_1: |
            ${{ inputs.before_plan_commands }}
          INPUT_PRE_EXEC_2: |
            ${{ inputs.before_shared_commands }}
          INPUT_POST_EXEC_0: |
            ${{ inputs.after_plan_commands }}
          INPUT_POST_EXEC_1: |
            ${{ inputs.after_shared_commands }}
          TF_VAR_organization_tag: ${{ steps.set-tags.outputs.TF_VAR_organization_tag }}
          TF_VAR_repository_tag: ${{ steps.set-tags.outputs.TF_VAR_repository_tag }}
          TF_VAR_commit_hash_tag: ${{ steps.set-tags.outputs.TF_VAR_commit_hash_tag }}
          TF_VAR_branch_tag: ${{ steps.set-tags.outputs.TF_VAR_branch_tag }}
          GIT_BRANCH: ${{ inputs.git_branch }}
        with:
          tf_path: "terraform"
          tg_version: ${{ inputs.tg_version }}
          tg_dir: "${{ inputs.environments_root }}/sandbox/${{ inputs.region }}/${{ inputs.env_id }}"
          tg_command: "plan -out=ephemeral-${{ inputs.region }}-${{ inputs.env_id }}.tfplan"

      - name: Deploy
        id: deploy
        uses: gruntwork-io/terragrunt-action@v3.1.0
        env:
          AWS_REGION: ${{ inputs.region }}
          INPUT_PRE_EXEC_0: |
            sudo apt update -yqq && sudo apt install python3 -yqq
          INPUT_PRE_EXEC_1: |
            ${{ inputs.before_deploy_commands }}
          INPUT_PRE_EXEC_2: |
            ${{ inputs.before_shared_commands }}
          INPUT_POST_EXEC_0: |
            ${{ inputs.after_deploy_commands }}
          INPUT_POST_EXEC_1: |
            ${{ inputs.after_shared_commands }}
          INPUT_POST_EXEC_99: |
            set -x
            ORIGINAL_PATH=$(pwd)
            cd $GITHUB_WORKSPACE/${{ inputs.environments_root }}/sandbox/${{ inputs.region }}/${{ inputs.env_id }}
            pwd
            terragrunt output -json
            terragrunt output -json | jq -r 'with_entries(select(.value.sensitive == false))' > outputs.json
            echo "terraform_outputs_json=$(base64 -w 0 outputs.json)" >> "$GITHUB_OUTPUT"
            cd $ORIGINAL_PATH
            set +x
          TF_VAR_organization_tag: ${{ steps.set-tags.outputs.TF_VAR_organization_tag }}
          TF_VAR_repository_tag: ${{ steps.set-tags.outputs.TF_VAR_repository_tag }}
          TF_VAR_commit_hash_tag: ${{ steps.set-tags.outputs.TF_VAR_commit_hash_tag }}
          TF_VAR_branch_tag: ${{ steps.set-tags.outputs.TF_VAR_branch_tag }}
          GIT_BRANCH: ${{ inputs.git_branch }}
        with:
          tf_path: "terraform"
          tg_version: ${{ inputs.tg_version }}
          tg_dir: "${{ inputs.environments_root }}/sandbox/${{ inputs.region }}/${{ inputs.env_id }}"
          tg_command: "apply ephemeral-${{ inputs.region }}-${{ inputs.env_id }}.tfplan"

      # - name: Gather Outputs
      #   id: get-outputs
      #   uses: gruntwork-io/terragrunt-action@v3.1.0
      #   env:
      #     AWS_REGION: ${{ inputs.region }}
      #     TF_VAR_organization_tag: ${{ steps.set-tags.outputs.TF_VAR_organization_tag }}
      #     TF_VAR_repository_tag: ${{ steps.set-tags.outputs.TF_VAR_repository_tag }}
      #     TF_VAR_commit_hash_tag: ${{ steps.set-tags.outputs.TF_VAR_commit_hash_tag }}
      #     TF_VAR_branch_tag: ${{ steps.set-tags.outputs.TF_VAR_branch_tag }}
      #     INPUT_PRE_EXEC_1: |
      #       unset TF_INPUT
      #       export TG_NON_INTERACTIVE=true
      #     GIT_BRANCH: ${{ inputs.git_branch }}
      #   with:
      #     tf_path: "terraform"
      #     tg_version: ${{ inputs.tg_version }}
      #     tg_dir: "${{ inputs.environments_root }}/sandbox/${{ inputs.region }}/${{ inputs.env_id }}"
      #     tg_command: |
      #       output -json

      # - name: Encode Outputs
      #   id: encode-outputs
      #   run: |
      #     RAW_OUTPUT=$(echo '${{ steps.get-outputs.outputs.tg_action_output }}')
      #     # Replace escaped newlines, angle brackets. Parse with JQ to exclude sensitive items and confirm the JSON is valid.
      #     CLEANED_OUTPUT=$(echo "$RAW_OUTPUT" | sed 's|%0A|\n|g' | sed 's|%3C|<|g' | sed 's|%3E|>|g' | jq -r 'with_entries(select(.value.sensitive == false))')
      #     ENCODED_OUTPUT=$(echo -n "$CLEANED_OUTPUT" | base64 -w 0)
      #     echo "$ENCODED_OUTPUT"
      #     echo "terraform_outputs=$ENCODED_OUTPUT" >> "$GITHUB_OUTPUT"
